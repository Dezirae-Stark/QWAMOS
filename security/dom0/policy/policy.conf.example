# QWAMOS Security Policy Configuration
# Edit this file to configure system security settings
# Changes are applied by qwamosd daemon

# Radio Isolation
RADIO_ISOLATION=on

# Radio Hardening Level
# - basic: Tor/I2P/DNSCrypt egress, IMS allowed
# - strict: Tor-only egress, IMS blocked
RADIO_HARDENING.level=strict

# Radio Idle Timeout (minutes)
# Radio automatically powers down after N minutes of inactivity
RADIO_IDLE_TIMEOUT_MIN=10

# Trusted Overlay
# Enable secure call UI overlays
TRUSTED_OVERLAY=on

# Verified Boot Enforcement
# - warn: Display warning on boot hash mismatch
# - enforce: Block unlock on boot hash mismatch
VERIFIED_BOOT_ENFORCE=enforce

# Remote Attestation
# - off: No remote attestation
# - warn: Upload boot hashes but don't enforce
# - enforce: Block unlock if attestation upload fails
REMOTE_ATTESTATION=warn

# Panic Gesture
# Enable Power+VolUp+Fingerprint emergency wipe gesture
PANIC_GESTURE=on

# Duress Profile
# Create decoy user profile for coercion scenarios
DURESS_PROFILE=on

# Kernel Hardening
# - default: Standard kernel security
# - strict: Lockdown mode + KASLR enforcement
KERNEL_HARDENING=strict

# Baseband Driver Disable
# - on: Air-gapped mode (no cellular radio)
# - off: Normal cellular operation
BASEBAND_DRIVER_DISABLE=off

# End-to-End Tunnel Policy
# - tor-only: All traffic via Tor
# - tor+vpn: Tor + VPN layering
# - custom: Custom egress configuration
E2E_TUNNEL_POLICY=tor-only

# Audit Upload
# - off: No audit log upload
# - tor-hidden: Upload logs to Tor hidden service
AUDIT_UPLOAD=off

# Policy signature (generated by qwamosd)
# Do not edit this line manually
SIG=ed25519:PLACEHOLDER_SIGNATURE

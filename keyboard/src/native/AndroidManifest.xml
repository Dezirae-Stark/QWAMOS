<?xml version="1.0" encoding="utf-8"?>
<!--
  QWAMOS SecureType Keyboard - Android Manifest

  CRITICAL PRIVACY FEATURES:
  - NO INTERNET permission (guaranteed zero telemetry)
  - Only VIBRATE permission (for haptic feedback)
  - Explicitly denies internet access
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.qwamos.securekeyboard">

    <!-- ONLY permission: Vibrate for haptic feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- EXPLICITLY DENY internet access (proof of zero telemetry) -->
    <uses-permission
        android:name="android.permission.INTERNET"
        tools:node="remove" />

    <!-- Biometric authentication -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".SecureKeyboardApplication"
        android:label="QWAMOS SecureType"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">

        <!-- Input Method Service -->
        <service
            android:name=".SecureKeyboardIME"
            android:label="QWAMOS SecureType Keyboard"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:label="SecureType Settings"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
